# Local Worker secrets for wrangler dev (gitignored, never commit real values)
# DASHBOARD_TOKEN is REQUIRED: the API fails closed without it.
TELEGRAM_BOT_TOKEN=your-telegram-bot-token
TELEGRAM_CHAT_ID=your-telegram-chat-id
DASHBOARD_TOKEN=your-dashboard-token
